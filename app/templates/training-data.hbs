<br>
{{#if model}}
  <h2>Images</h2>
  <div class="list-group">
    {{#each model as |label|}}
    {{#link-to "label" label.id class="list-group-item list-group-item-action"}}
    {{label.label}}
    {{/link-to}}
    {{/each}}
  </div>
{{else}}
  <h2>No images uploaded!</h2>
{{/if}}
